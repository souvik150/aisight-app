import "package:flutter/material.dart";

class Iternirary extends StatelessWidget {
  const Iternirary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Iternirary"),
      ),
    );
  }
}
